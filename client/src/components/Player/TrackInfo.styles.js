import { makeStyles } from '@material-ui/styles';

const styles = makeStyles((theme) => ({
  songName: {
    fontWeight: 'bold',
    fontSize: 16,
  },
  artistNames: {
    fontSize: 12,
  },
}));

export default styles;

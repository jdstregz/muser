import { makeStyles } from '@material-ui/styles';

const styles = makeStyles((theme) => ({
  root: {
    height: 65,
    backgroundColor: '#fff',
  },
  playButtonContainer: {
    textAlign: 'center',
    height: 65,
  },
  playButton: {
    height: 40,
    width: 40,
  },
}));

export default styles;
